import XCTest
@testable import PackageDeliveryKit

final class PackageDeliveryKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PackageDeliveryKit().text, "Hello, World!")
    }
}
